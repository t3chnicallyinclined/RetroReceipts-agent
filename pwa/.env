# API origin for the PWA's server calls. Empty = same-origin: at nobd.net/app the client hits
# nobd.net/skinsync/* (nginx proxies it). Non-secret, build-time default — TRACKED (see .gitignore)
# so a fresh clone builds without setup. Per-environment overrides go in .env.local (gitignored).
PUBLIC_API_BASE=
